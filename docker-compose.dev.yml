services:
  # Postgres reuses an externally-managed container (e.g. `postgres-17` on host 5432).
  # See `.env` DATABASE_URL.
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - '1025:1025'  # SMTP
      - '8025:8025'  # web UI
